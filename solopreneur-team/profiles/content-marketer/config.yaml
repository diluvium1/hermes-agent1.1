# content-marketer — model config
# Distribution-owned but preserved on update unless --force-config.

model:
  default: "google/gemini-2.5-flash"   # high-volume drafting; switch to Claude with /model for polish
  provider: "auto"

agent:
  max_turns: 40

memory:
  memory_enabled: true
  user_profile_enabled: true
