# sales-crm — model config
# Distribution-owned but preserved on update unless --force-config.

model:
  default: "google/gemini-3-flash-preview"   # research + drafting volume; switch with /model
  provider: "auto"

agent:
  max_turns: 40

memory:
  memory_enabled: true
  user_profile_enabled: true
